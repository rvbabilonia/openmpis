<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE sqlMap PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN" "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap namespace="User">
    <!--resultMap id="userResult" class="com.googlecode.openmpis.model.User">
        <result property="id" column="ID"/>
        <result property="groupID" column="GROUPID"/>
        <result property="username" column="USERNAME"/>
        <result property="password" column="PASSWORD"/>
        <result property="firstName" column="FIRSTNAME"/>
        <result property="middleName" column="MIDDLENAME"/>
        <result property="lastName" column="LASTNAME"/>
        <result property="email" column="EMAIL"/>
        <result property="designation" column="DESIGNATION"/>
        <result property="agency" column="AGENCY"/>
        <result property="number" column="NUMBER"/>
        <result property="ipAddress" column="IPADDRESS"/>
        <result property="lastLogin" column="LASTLOGIN"/>
        <result property="date" column="DATE"/>
        <result property="status" column="STATUS"/>
        <result property="question" column="QUESTION"/>
        <result property="answer" column="ANSWER"/>
        <result property="creatorID" column="CREATORID"/>
    </resultMap-->
    
    <!-- Use primitive wrapper type (e.g. Integer) as parameter and allow results to
    be auto-mapped results to Person object (JavaBean) properties -->
    <!--select id="getUser" resultMap="userResult"-->
    <select id="getUser" parameterClass="String" resultClass="com.googlecode.openmpis.model.User">
        SELECT
        ID as id,
        GROUPID as groupId,
        USERNAME as username,
        PASSWORD as password,
        FIRSTNAME as firstName,
        LASTNAME as lastName,
        EMAIL as email,
        STATUS as status,
        QUESTION as question,
        ANSWER as answer
        FROM USER
        WHERE USERNAME = #username# AND STATUS = '0'
    </select>
    
    <select id="viewUser" parameterClass="int" resultClass="com.googlecode.openmpis.model.User">
        SELECT
        ID as id,
        GROUPID as groupId,
        USERNAME as username,
        FIRSTNAME as firstName,
        MIDDLENAME as middleName,
        LASTNAME as lastName,
        EMAIL as email,
        DESIGNATION as designation,
        AGENCY as agency,
        NUMBER as number,
        IPADDRESS as ipAddress,
        LASTLOGIN as lastLogin,
        DATE as date,
        STATUS as status,
        CREATORID as creatorId,
        QUESTION as question,
        ANSWER as answer
        FROM USER
        WHERE ID = #id#
    </select>
    
    <select id="getUsers" resultClass="com.googlecode.openmpis.model.User">
        SELECT
        ID as id,
        GROUPID as groupId,
        USERNAME as username,
        FIRSTNAME as firstName,
        MIDDLENAME as middleName,
        LASTNAME as lastName,
        EMAIL as email,
        DESIGNATION as designation,
        AGENCY as agency,
        NUMBER as number,
        IPADDRESS as ipAddress,
        LASTLOGIN as lastLogin,
        DATE as date,
        STATUS as status,
        CREATORID as creatorId
        FROM USER
    </select>
        
    <select id="checkUsername" parameterClass="com.googlecode.openmpis.model.User" resultClass="String">
        SELECT
        USERNAME as username
        FROM USER
        WHERE USERNAME = #username# AND ID != #id#
    </select>
        
    <select id="checkEmail" parameterClass="com.googlecode.openmpis.model.User" resultClass="String">
        SELECT
        EMAIL as email
        FROM USER
        WHERE EMAIL = #email# AND ID != #id# 
    </select>

    <!-- Use Person object (JavaBean) properties as parameters for insert. Each of the
    parameters in the #hash# symbols is a JavaBeans property. -->
    <insert id="insertUser" parameterClass="com.googlecode.openmpis.model.User">
        INSERT INTO USER
        (GROUPID, USERNAME, PASSWORD, FIRSTNAME, MIDDLENAME, LASTNAME, EMAIL, DESIGNATION,
        AGENCY, NUMBER, DATE, STATUS, CREATORID, QUESTION)
        VALUES
        (#groupId#, #username#, MD5(#password#), #firstName#, #middleName#, #lastName#,
        #email#, #designation#, #agency#, #number#, #date#, #status#, #creatorId#, #question#)
    </insert>

    <!-- Use Person object (JavaBean) properties as parameters for update. Each of the
    parameters in the #hash# symbols is a JavaBeans property. -->
    <update id="updateUser" parameterClass="com.googlecode.openmpis.model.User">
        UPDATE USER SET
        GROUPID = #groupId#,
        USERNAME = #username#,
        PASSWORD = MD5(#password#),
        FIRSTNAME = #firstName#,
        MIDDLENAME = #middleName#,
        LASTNAME = #lastName#,
        EMAIL = #email#,
        DESIGNATION = #designation#,
        AGENCY = #agency#,
        NUMBER = #number#,
        IPADDRESS = #ipAddress#,
        LASTLOGIN = #lastLogin#,
        DATE = #date#,
        STATUS = #status#,
        QUESTION = #question#,
        ANSWER = #answer#,
        CREATORID = #creatorId#
        WHERE ID = #id#
    </update>
    
    <update id="updatePassword" parameterClass="com.googlecode.openmpis.model.User">
        UPDATE USER SET
        PASSWORD = MD5(#password#)
        WHERE ID = #id#
    </update>
    
    <update id="updateLogin" parameterClass="com.googlecode.openmpis.model.User">
        UPDATE USER SET
        IPADDRESS = #ipAddress#,
        LASTLOGIN = #lastLogin#
        WHERE ID = #id#
    </update>

    <!-- Use Person object (JavaBean) “id” properties as parameters for delete. Each of the
    parameters in the #hash# symbols is a JavaBeans property. -->
    <delete id="deleteUser" parameterClass="int">
        DELETE FROM USER
        WHERE ID = #id#
    </delete>
</sqlMap>